apiVersion: v1
kind: Service
metadata:
  name: result-canary
  labels:
    app.kubernetes.io/name: result
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: voting-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: result
    # Argo Rollouts will add the rollouts-pod-template-hash selector
