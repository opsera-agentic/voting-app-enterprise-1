apiVersion: v1
kind: Service
metadata:
  name: result-preview
  labels:
    app.kubernetes.io/name: result
    app.kubernetes.io/component: frontend
    service-type: preview
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: result
